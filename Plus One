class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        if len(digits)==0: return 1
        num=0
        for i in digits:
            num=num*10+(i)
        num=num+1
        res=map(int,str(num))
        return res
